// src/routes/login/inquiries/+page.server.js

/** @type {import('./$types').PageLoad} */
export async function load({ locals }) {
	// Optional: You can add server-side checks here
	// For now, we'll handle auth entirely client-side with Firebase
	return {};
}